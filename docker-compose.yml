services:
  code-server:
    build: .
    container_name: copilot-code-server

    privileged: true

    volumes:
      # Persist entire agent home directory (includes config, extensions, workspace, everything)
      - agent-home:/home/agent

      # Persist Docker-in-Docker state (images/containers started by Aspire)
      - docker-data:/var/lib/docker

      # Mount MCP configuration (managed externally)
      - ./copilot:/home/agent/.copilot

      # Mount VS Code settings
      - ./vscode-settings.json:/home/agent/.local/share/code-server/User/settings.json

      # Mount cli-mcp-mapper commands
      - ./commands.json:/home/agent/.config/cli-mcp-mapper/commands.json

    environment:
      # Per-developer Git configuration
      - GIT_USERNAME=${GIT_USERNAME}
      - GIT_EMAIL=${GIT_EMAIL}
      
      # Repository checkout configuration
      - REPO_URL=git@ssh.dev.azure.com:v3/TDRRecoveryTrac/RecoveryTrac/Aspire
      - REPO_FOLDER=RecoveryTrac
      
      # Shell theme preference
      - ZSH_THEME=jonathan
      
      # Aspire Dashboard binding (0.0.0.0 allows external access)
      - ASPNETCORE_URLS=https://0.0.0.0:17143

    ports:
      # code-server
      - "8080:8080"
      # Aspire Dashboard
      - "17143:17143"
      # WebApi
      - "7107:7107"

volumes:
  agent-home:
  docker-data:
