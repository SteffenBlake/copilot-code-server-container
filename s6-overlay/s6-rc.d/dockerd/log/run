#!/command/with-contenv bash
set -euo pipefail

# - T: add timestamp to each line
# - b: block buffering (better for streaming)
# Then prefix with [dockerd].
exec s6-log -b T \
  | /usr/local/share/copilot-code-server-container/log-prefix.sh dockerd
